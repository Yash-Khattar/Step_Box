import 'package:flutter/material.dart';

class StatsScreen extends StatelessWidget {
  StatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Stats Screen")),
    );
  }
}
